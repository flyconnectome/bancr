% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{banc_lm_links}
\alias{banc_lm_links}
\title{Pre-minted Spelunker viewer URLs for the BANC light-microscopy receptor library}
\format{
A \code{tibble} with one row per LM layer and columns:
\describe{
\item{\code{source}}{Upstream dataset citation (e.g.
\code{"Kondo et al. 2020"}).}
\item{\code{gene}}{Receptor / peptide / gene short name (e.g.
\code{"GluRIIA"}, \code{"5-HT2A"}, \code{"CapaR"}).}
\item{\code{sample}}{Sample identifier within the gene
(Kondo's \code{no1} / \code{no2} prepared specimens).}
\item{\code{ngl_link}}{Spelunker URL of the form
\code{https://spelunker.cave-explorer.org/#!middleauth+...nglstate/api/v1/<id>},
opening a scene that overlays the LM layer on the canonical
public BANC scene.}
}
}
\source{
Built by \code{data-raw/make_banc_lm_links.R} from the
master registry at
\code{gs://lee-lab_brain-and-nerve-cord-fly-connectome/light_level/registry.json}.
Re-run the script (\verb{Rscript data-raw/make_banc_lm_links.R}) to
pick up new layers or re-mint URLs with different defaults.
}
\usage{
banc_lm_links
}
\description{
A four-column flat record of pre-minted Spelunker
(\code{spelunker.cave-explorer.org}) viewer URLs for every
light-microscopy receptor / neuropeptide / orphan-GPCR layer
currently mirrored under
\code{gs://lee-lab_brain-and-nerve-cord-fly-connectome/light_level/}.
Each layer is a Kondo et al. 2020 endogenously-tagged receptor
GFP stack bridged via CMTK + saalfeldlab template-building +
Elastix into the BANC voxel grid (see
\code{neuronbridger::lm_to_banc_layer}). Each
\code{ngl_link} is a state ID resolvable forever by the BANC
state server, so this table is a stable shareable index.

For ad-hoc / interactive use, prefer
\code{bancr:::banc_lm_volumes()} (the live registry browser) +
\code{\link{banc_lm_scene}} (mints a fresh URL with custom
render settings); \code{banc_lm_links} is the canned snapshot
for sharing or scripting.
}
\examples{
\dontrun{
# Browse all glutamate-receptor layers
subset(banc_lm_links, grepl("GluR|Nmdar|GluCl|mGluR|KaiR", gene))

# Open one in the browser
utils::browseURL(banc_lm_links$ngl_link[
  banc_lm_links$gene == "GluRIIA" & banc_lm_links$sample == "no1"])
}
}
\keyword{datasets}
